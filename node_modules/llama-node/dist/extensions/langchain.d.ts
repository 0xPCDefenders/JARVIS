import { Embeddings, EmbeddingsParams } from 'langchain/embeddings/base';
import { LLama as LLM } from '../index.js';
import '../llm.d-120996aa.js';

declare class LLamaEmbeddings extends Embeddings {
    llm: LLM;
    constructor(params: EmbeddingsParams, llm: LLM);
    embedDocuments(documents: string[]): Promise<number[][]>;
    embedQuery(document: string): Promise<number[]>;
}

export { LLamaEmbeddings };
